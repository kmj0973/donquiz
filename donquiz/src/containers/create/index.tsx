"use client";

import { getCookie } from "@/global/cookie";
import { redirect } from "next/navigation";

const Create = () => {
  return (
    <>
      <div>create</div>
    </>
  );
};

export default Create;
