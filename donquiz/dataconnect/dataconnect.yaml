specVersion: "v1beta"
serviceId: "donquiz"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "donquiz-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
